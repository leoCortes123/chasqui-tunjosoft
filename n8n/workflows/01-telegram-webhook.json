{
  "id": "chasqui_tunjosoftTelegram",
  "name": "Chasqui TunjoSoft — Webhook de Telegram",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "chasqui-tunjosoft-telegram",
        "responseMode": "onReceived",
        "responseData": "noData",
        "options": {}
      },
      "id": "a1000000-0000-4000-8000-000000000001",
      "name": "Webhook Telegram",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -620,
        0
      ],
      "webhookId": "chasqui-tunjosoft-telegram",
      "notes": "responseMode=onReceived: n8n contesta 200 apenas recibe, antes de procesar. Es lo que garantiza el requisito de responder en menos de 1 segundo (§2.2.3). Telegram no reintenta y el trabajo sigue en segundo plano."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT CASE\n         WHEN registrar_update_telegram($1::jsonb)\n         THEN bot_manejar_update($1::jsonb)\n         ELSE jsonb_build_object('acciones', '[]'::jsonb, 'duplicado', true)\n       END AS respuesta;",
        "options": {
          "queryReplacement": "={{ JSON.stringify($json.body) }}"
        }
      },
      "id": "a1000000-0000-4000-8000-000000000002",
      "name": "Registrar y resolver",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        -400,
        0
      ],
      "credentials": {
        "postgres": {
          "id": "chasqui-tunjosoft-postgres",
          "name": "Chasqui TunjoSoft Postgres"
        }
      },
      "notes": "Una sola ida a la base. registrar_update_telegram descarta los updates repetidos por update_id (§2.2.2); si es nuevo, bot_manejar_update devuelve la lista de acciones. Toda la lógica vive en Postgres: n8n no decide nada."
    },
    {
      "parameters": {
        "jsCode": "// Traduce las acciones que devolvió Postgres a llamadas de la Bot API.\n// No hay lógica de negocio aquí: sólo cambio de formato.\nconst salida = [];\n\nfor (const item of $input.all()) {\n  const respuesta = item.json.respuesta ?? {};\n  const acciones = respuesta.acciones ?? [];\n\n  for (const a of acciones) {\n    let metodo;\n    let cuerpo;\n\n    if (a.tipo === 'responder_callback') {\n      if (!a.callback_query_id) continue;\n      metodo = 'answerCallbackQuery';\n      cuerpo = { callback_query_id: a.callback_query_id };\n      if (a.texto) cuerpo.text = a.texto;\n    } else if (a.tipo === 'enviar') {\n      metodo = 'sendMessage';\n      cuerpo = { chat_id: a.chat_id, text: a.texto, parse_mode: 'HTML' };\n    } else if (a.tipo === 'editar') {\n      metodo = 'editMessageText';\n      cuerpo = {\n        chat_id: a.chat_id,\n        message_id: a.message_id,\n        text: a.texto,\n        parse_mode: 'HTML',\n      };\n    } else {\n      continue;\n    }\n\n    if (a.botones && a.botones.length > 0) {\n      cuerpo.reply_markup = {\n        inline_keyboard: a.botones.map((fila) =>\n          fila.map((b) => ({ text: b.t, callback_data: b.d })),\n        ),\n      };\n    }\n\n    salida.push({ json: { metodo, cuerpo } });\n  }\n}\n\nreturn salida;"
      },
      "id": "a1000000-0000-4000-8000-000000000003",
      "name": "Acciones a Bot API",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -180,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://api.telegram.org/bot{{ $env.TELEGRAM_BOT_TOKEN }}/{{ $json.metodo }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.cuerpo) }}",
        "options": {
          "timeout": 10000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      },
      "id": "a1000000-0000-4000-8000-000000000004",
      "name": "Enviar a Telegram",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        40,
        0
      ],
      "onError": "continueRegularOutput",
      "notes": "neverError + continueRegularOutput: editMessageText devuelve 400 'message is not modified' cuando el usuario pulsa dos veces el mismo botón. Es normal y no debe abortar el resto de las acciones."
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT marcar_update_procesado($1::bigint);",
        "options": {
          "queryReplacement": "={{ $('Webhook Telegram').first().json.body.update_id }}"
        }
      },
      "id": "a1000000-0000-4000-8000-000000000005",
      "name": "Marcar procesado",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        260,
        0
      ],
      "executeOnce": true,
      "credentials": {
        "postgres": {
          "id": "chasqui-tunjosoft-postgres",
          "name": "Chasqui TunjoSoft Postgres"
        }
      }
    }
  ],
  "connections": {
    "Webhook Telegram": {
      "main": [
        [
          {
            "node": "Registrar y resolver",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Registrar y resolver": {
      "main": [
        [
          {
            "node": "Acciones a Bot API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Acciones a Bot API": {
      "main": [
        [
          {
            "node": "Enviar a Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enviar a Telegram": {
      "main": [
        [
          {
            "node": "Marcar procesado",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Bogota",
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all",
    "saveExecutionProgress": false
  }
}